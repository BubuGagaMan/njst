// these routes are in a route group - by using the ()s around the parent folder (auth)
// when accessing the url, the path does not have to include the ()s wrapped folder, but the 
// pages will still be able to share their parent's layout
export default async function Login() {
    return <h1>Register</h1>

}